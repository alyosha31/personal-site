---
import { getCollection } from 'astro:content';
import SiteLayout from '../../layouts/SiteLayout.astro';

const posts = (await getCollection('posts'))
  .filter((post) => !post.data.draft)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());
---

<SiteLayout
  title="Writing — Alyosha"
  description="Writing by Alyosha."
  bodyClass="document-page"
>
      <nav aria-label="Breadcrumb"><a href="/">Home</a><span>/</span><span>Writing</span></nav>
      <header>
        <h1>Writing</h1>
        <p>Notes from the messy middle.</p>
      </header>
      <div class="document-list">
        {posts.map((post) => (
          <a href={`/writing/${post.id}/`}>
            <time datetime={post.data.date.toISOString()}>{post.data.date.toISOString().slice(0, 10)}</time>
            <div><h2>{post.data.title}</h2><p>{post.data.description}</p></div>
            <span>Read →</span>
          </a>
        ))}
      </div>
</SiteLayout>
